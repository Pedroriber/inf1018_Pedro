Notas: entendo a correspondencia entre eles, fiz algumas anotações
no cõdigo

/*
#include <stdio.h>

char S2[] = {65, 108, 111, 32, 123, 103, 97, 108, 101, 114, 97, 125, 33, 0};

int main (void) {
  char *pc = S2;
  while (*pc)
    printf ("%c", *pc++);
  printf("\n");
  return 0;
}*/

1- função printa caracteres do codigo ASCII.

.data
S2:    .byte (pois é vetor de char) 65, 108, 111, 32, 123, 103, 97, 108, 101, 114, 97, 125, 33, 0
Sf:  .string "%c"    /* primeira string de formato para printf */
Sf2: .string "\n"    /* segunda string de formato para printf */

.text
.globl  main
main:


  movq  $S2, %r12  /* r12 = &S2  (registra o vetor)*/ 
  - movq pois ponteiro tem 8 bytes
  - $ é endereço de S2

L1:
  cmpb  $0, (%r12)  /* if (*pc == 0) ? */ 
  - condição do while
  - atenção aos parentes: conteudo do endereço apontado por r12
  je  L2          /* goto L2 */

  movsbl  (%r12), %eax    /* eax = *r12 (estendendo o byte para 32 bits */
  - estende pois o printf espera um int de 32 bits
/*************************************************************/
/* este trecho imprime o valor de %eax (estraga %eax)  */
  movq    $Sf, %rdi    /* primeiro parametro (ponteiro)*/
  movl    %eax, %esi   /* segundo parametro  (inteiro) */
  movl  $0, %eax
  call  printf       /* chama a funcao da biblioteca */
/*************************************************************/

  addq  $1, %r12  /* r12 += 1; */
  jmp  L1         /* goto L1; */

L2:  
/*************************************************************/
/* este trecho imprime o \n (estraga %eax)                  */
  movq    $Sf2, %rdi    /* primeiro parametro (ponteiro)*/
  movl  $0, %eax
  call  printf       /* chama a funcao da biblioteca */
/*************************************************************/

/***************************************************************/
/* mantenha este trecho aqui e nao mexa - finalizacao!!!!      */
  movq  $0, %rax  /* rax = 0  (valor de retorno) */
  movq    -16(%rbp), %r12 /* recupera r12 */
  movq    -8(%rbp), %rbx  /* recupera rbx */
  leave
  ret      
/***************************************************************/

2) curto cicuito:
  cmpb  $123, (%r12)
  je  increm
  cmpb  $125, (%r12)
  je  increm

se o primeiro der igual, ele não testa o segunfo (curto)

3) não dá pra usar eax pois é destruido pelo printf

4) movl    S2(,%ebx,4), %edx   # edx = nums[i]
S2(,%ebx,4) - acessa um elememto do vetor

- mudar o print da finalização, adicionando   movl    %eax, %esi
