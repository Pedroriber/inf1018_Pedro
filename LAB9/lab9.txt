1) função add: não precisa de calle-saved mo prologp, pois a função não chamada outras funções
que destroem registradores

2) para a função fat usamos um calle saved pois a função é recursiva:
fat:
    pushq %rbp
    movq  %rsp, %rbp
    subq  $16, %rsp
    movq  %rbx, -8(%rbp)

3) usamos calle-saved(rbx e r12), pois há duas variaveis locais que precisam ficar salvas durante o loop
mas daria pra fazer sem o calle-saved e apenas com caller-saved, pois a função não chama outras funções

  # calcular &a[i]
  movslq %ebx, %rax     # rax = i
  imulq  $4, %rax       # i * 4
  addq   %rdi, %rax     # rax = &a[i]

  ao inves desse calculo, posso (%rdi) - endereco do inicio do array
  e deipois incrementar 
    addq   $4, %rdi
  mas o exercicio pede o calculo de index
  